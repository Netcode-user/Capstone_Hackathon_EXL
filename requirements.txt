# --- Core (required, lightweight, install fine everywhere) ---
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
python-dotenv>=1.0
numpy>=1.26
jinja2>=3.1
python-multipart>=0.0.9

# --- LLM ---
anthropic>=0.34          # used if ANTHROPIC_API_KEY is set; app degrades gracefully without it

# --- Optional upgrades (auto-detected, app works fine without these) ---
sentence-transformers>=2.7   # better embeddings; falls back to hashing embedder if absent
faiss-cpu>=1.8                # faster ANN vector search; falls back to NumPy brute force if absent
kafka-python>=2.0             # real Kafka; falls back to in-process MockEventBus if absent / EVENT_BUS_MODE=mock
